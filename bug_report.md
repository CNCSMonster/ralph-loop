# bug report

## 1. init 异常
状态: ✅ 已解决 (迭代 1 修复)
init 后, 用status子命令分析任务完成情况，得到————
约束规则提到"总任务数: 2，已完成: 1"，但 `plan.md` 中实际上**没有任何任务定义**。但是刚初始化的时候，应该只是创建了文档结构，还没有定义任务才对，所以总任务数不应该为2,
已完成的任务量应该为0,才对，

**修复说明**: 
- 问题根源: `get_task_stats` 函数的正则表达式不够精确,匹配到了注释块中的示例任务行
- 修复方式: 修改正则只匹配以 `- [ ]` 或 `- [x]` 开头的实际任务行,并添加 `|| true` 处理 grep 无匹配时的退出码
- 同时修改了 init 生成的 plan.md 模板,移除注释块中的示例任务行
